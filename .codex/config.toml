# .codex/config.toml

# (선택) 기본 모델/프로바이더는 Codex 문서의 config reference에 맞춰 조정
# 문서: Config basics / Configuration reference 참고 :contentReference[oaicite:7]{index=7}

[project]
# Codex가 읽고/수정해도 되는 루트 폴더(기본은 현재 디렉토리)
root = "."

[agent]
# 보수적으로 시작: 변경 전 항상 계획/확인 요청하도록 유도
style = "careful"

[sandbox]
# Windows에서 특히 중요: 기본적으로 작업 폴더 밖 쓰기 제한/네트워크 제한 안내가 있음 :contentReference[oaicite:8]{index=8}
